#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i,n) for(ll i = 0; i < (n); ++i)
using P = pair<ll,ll>;
int main(){
    ll N;
    cin >> N;
    rep(i,N){
        string A,B;
        cin >> A >> B;
        ll cont[A.size()][B.size()];
        rep(j,A.size()){
            rep(k,B.size()){
                if(A[j] == B[k]){
                    cont[j][k] =
                }
            }
        }
    }
}