#include <bits/stdc++.h>
using ll = long long;
#define rep(i,n) for(ll i = 0; i < (n); ++i)
using namespace std;
using P = pair<ll,ll>;
int main(){
    ll a,b,c,d;
    cin >> a >> b >> c >> d;
    cout << (a+b)*(c-d) << endl;
    cout << "Takahashi" << endl;
}