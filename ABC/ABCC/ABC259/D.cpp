#include <bits/stdc++.h>
using ll = long long;
#define rep(i,n) for(ll i = 0; i < (n); ++i)
using namespace std;
using P = pair<ll,ll>;
int main(){
    ll N;
    ll sx,sy,tx,ty;
    cin >> sx >> sy >> tx >> ty;
    ll X[N];
    ll Y[N];
    ll R[N];
    rep(i,N){
        cin >> X[i] >> Y[i] >> R[i];
    }

    
}