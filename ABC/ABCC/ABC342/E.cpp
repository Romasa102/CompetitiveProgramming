#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i,n) for(ll i = 0; i < (n); ++i)
#define repp(i,o,n) for(ll i = o; i < (n); ++i)
using P = pair<ll,ll>;

int main(){
    ll N,M;cin >> N >> M;
    ll l[M],d[M],k[M],c[M],A[M],B[M];
    rep(i,M)cin >> l[i] >> d[i] >> k[i] >> c[i] >> A[i] >> B[i];
    vector<ll> time(N);
    rep(i,N)time[i] = -(1LL<<60);
    queue<ll> Q;Q.push()
}
