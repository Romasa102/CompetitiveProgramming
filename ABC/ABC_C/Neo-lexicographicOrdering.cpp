#include <bits/stdc++.h>
using ll = long long;
#define rep(i,n) for(ll i = 0; i < (n); ++i)
using namespace std;
using P = pair<ll,ll>;
int main(){
    string S;
    string Normal = "abcdefghijklmnopqrstuvwxyz";
    cin >> S;
    ll N;
    cin >> N;
    vector<string> S[N];
    rep(i,N){
        cin >> S[i];
    }
    rep(i,N){
    }
}